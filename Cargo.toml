[workspace]
resolver = "3"
members = [
    "API",
    "CLI",
    "core",
    "MCP",
    "xtask",
    "GUI/src-tauri",
    "tests"
    ]

package.edition = "2021"

[profile.release]
opt-level = 3
lto = "thin"
strip = true


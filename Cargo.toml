[workspace]
resolver = "3"
members = [
    "HTTP",
    "CLI",
    "core",
    "MCP",
    "GUI/src-tauri",
    "tests",
    "FFI"]

package.version = "1.11.0"
package.edition = "2021"

[profile.release]
opt-level = 3
lto = "thin"

